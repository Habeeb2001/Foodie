const food = require("./components/FoodListComponent/FoodsMockApi")

// console.log(
//     food.filter((item, idx) => item[idx].type === "heavy" || item[idx].type === "light"||  item[idx].type === "beverage")
// )
;
  console.log(Foods)